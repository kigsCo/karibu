---
description: Regenerate supabase/seed.sql from the prototype constants in src/KaribuApp.jsx.
argument-hint:
allowed-tools: Read, Write, Edit, Bash, Grep, Glob
---

Invoke the **`seed-data`** skill and (re)generate `supabase/seed.sql` from the prototype's hardcoded constants in `src/KaribuApp.jsx`. Read the root `CLAUDE.md` and `supabase/CLAUDE.md` first, then read `src/KaribuApp.jsx` for the current source data and `supabase/migrations/20260601000001_core_schema.sql` for the exact column names.

Follow the skill's mapping table: `cities` → `cities`, `categories`/`subTypes` → `categories`/`sub_types`, `recommended` + `salonsList` → `businesses`, `reviewsSample` → `reviews`, `guides` → `guides`. Apply the field mappings (`reviews` → `review_count`, `price` → `price_range`, `badge` → `tier`, etc.) and the `badge`→`tier` rule.

Golden rule: **reference every foreign key by a slug subquery, never a hardcoded UUID** (UUIDs are generated by `uuid_generate_v4()` at insert time). Insert parents before children (categories before sub_types; cities before businesses/guides). The file must run cleanly at the end of `supabase db reset` from a fresh DB, and respect constraints (e.g. `reviews.body` length CHECK).

When done, report what changed in `seed.sql` and note it should be verified with `supabase db reset` locally.
